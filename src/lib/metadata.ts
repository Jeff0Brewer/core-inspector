type SectionIdMetadata = {
    ids: Array<string>
}

type CoreMetadata = {
    numSection: number,
    topDepth: number,
    bottomDepth: number
}

export type {
    SectionIdMetadata,
    CoreMetadata
}
